@model List<ModelEF.Model.Category>

<div class="aside">
	<h3 class="aside-title">Loại sản phẩm</h3>
	<div>

		@foreach (var item in Model)
		{
			<div class="checkbox">
				<input type="checkbox">
				<label>
					@item.Name
				</label>
			</div>
		}
	</div>
</div>
